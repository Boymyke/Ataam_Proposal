[build]
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/api/vault"
  to = "/.netlify/functions/vault"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "no-referrer"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
